-- Script that computes the average score of all records in the second_table table
-- Select the average score from the second_table table, with column name "average"
SELECT AVG(score) AS average
FROM second_table;
